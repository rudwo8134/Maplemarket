'use strict';
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var fs_1 = __importDefault(require("fs"));
var path_1 = __importDefault(require("path"));
var config_1 = __importDefault(require("../../../config"));
var tokenDir = config_1.default.tokenDir, tokenFile = config_1.default.tokenFile;
var AuthStrategy = /** @class */ (function () {
    function AuthStrategy() {
    }
    AuthStrategy.prototype.logout = function () {
        return this.removeToken();
    };
    AuthStrategy.prototype.saveToken = function (userToken) {
        if (userToken) {
            var tokenPath = path_1.default.join(tokenDir, tokenFile);
            try {
                fs_1.default.mkdirSync(tokenDir, { recursive: true, mode: 493 });
                fs_1.default.writeFileSync(tokenPath, userToken, { encoding: 'utf8', mode: 420 });
            }
            catch (err) {
                return false;
            }
            return true;
        }
        return false;
    };
    AuthStrategy.prototype.removeToken = function () {
        var tokenPath = path_1.default.join(tokenDir, tokenFile);
        try {
            fs_1.default.unlinkSync(tokenPath);
            return null;
        }
        catch (e) {
            if (e.code === 'ENOENT')
                return 'You are not logged in.';
            return "Logout failed: " + e.message;
        }
    };
    return AuthStrategy;
}());
exports.default = AuthStrategy;
